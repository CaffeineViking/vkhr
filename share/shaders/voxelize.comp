#version 460 core

layout(local_size_x = 8, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) buffer Data {
    float numbers[];
};

void main() {
    numbers[gl_LocalInvocationIndex] = pow(numbers[gl_LocalInvocationIndex], 2.0f);
}
